rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

service google.firestore.v1.documents {   
  match /projects/{project}/databases/{database}/documents
  {
    allow read, write: if true;
  }
}